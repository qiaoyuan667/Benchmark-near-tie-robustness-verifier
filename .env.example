# Local data and recomputation root, not the public artifact directory.
FAMILY_DIF_PROJECT_ROOT=../audit-data

# Directory containing the licensed mapping/question sources used in data alignment.
FAMILY_DIF_SOURCE_DATA_ROOT=../authorized-question-data

# Never store provider API keys here; use your backend's environment variables.
# Fresh annotation uses the explicit --backend-command argument.
